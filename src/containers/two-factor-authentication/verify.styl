.container {
  h2 {
    text-align: center;
  }
  p {
    color: rgba(0, 0, 0, 0.54);
    margin: 10px auto;
    font-size: 14px;
    text-align: center;
  }
  .code {
    input {
      margin-bottom: 30px;
      font-size: 24px;
      letter-spacing: 1px;
      height: auto;
    }
    text-align: center;
    margin-top: 30px;
  }
  .next {
    float: right;
  }
}